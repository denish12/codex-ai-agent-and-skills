interface:
  display_name: "Unit Economics"
  short_description: "Unit economy analysis — CAC, LTV, margin, payback period, sensitivity"
  default_prompt: "Use $unit-economics when the task relates to the \"Unit Economics\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
