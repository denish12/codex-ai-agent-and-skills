version: 1
name: "board"
display_name: "Analytics Pipeline Project Board"
description: "Management of the analytics pipeline project board — initialization, statuses, event log, blocker escalation"
default_prompt: "Use $board when the task relates to the \"Analytics Pipeline Project Board\" skill."
triggers:
  - "Analytics pipeline project board"
  - "analytics pipeline project board management — initialization, statuses, event log, blocker escalation"
  - "Analytics pipeline project board"
  - "Analytics pipeline project board"
  - "board"
capabilities:
  - "board"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "ru"
  available_locales:
    - "ru"
    - "en"
