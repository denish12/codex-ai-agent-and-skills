interface:
  display_name: "Product Vision"
  short_description: "Видение продукта — for whom / why / what's unique / 2-3 year horizon / guiding p"
  default_prompt: "Используй $product-vision, когда задача относится к навыку \"Product Vision\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
