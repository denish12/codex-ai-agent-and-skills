interface:
  display_name: "User Story"
  short_description: "Writing user stories per INVEST — As a / I want / So that, separation of buyer/end-user"
  default_prompt: "Use $user-story when the task relates to the skill \"User Story\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
