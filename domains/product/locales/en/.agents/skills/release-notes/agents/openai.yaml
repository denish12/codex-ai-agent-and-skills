interface:
  display_name: "Release Notes (B2B SaaS)"
  short_description: "B2B customer-facing release notes — what's new, why it matters, breaking changes, migration steps"
  default_prompt: "Use $release-notes when the task relates to the skill \"Release Notes (B2B SaaS)\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
