version: 1
name: "handoff"
display_name: "Handoff"
description: "Transferring artifacts between gates and sessions — structured format with decision context"
default_prompt: "Use $handoff when the task relates to the skill \"Handoff — Transferring Context Between Gates and Sessions\"."
triggers:
  - "handoff"
  - "session handoff"
capabilities:
  - "handoff"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "en"
