interface:
  display_name: "Epic Breakdown"
  short_description: "Epic decomposition into stories with dependencies, size estimates (S/M/L/XL), critical path"
  default_prompt: "Use $epic-breakdown when the task relates to the skill \"Epic Breakdown\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
