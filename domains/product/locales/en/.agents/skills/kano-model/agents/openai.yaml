interface:
  display_name: "Kano Model"
  short_description: "Feature classification — must-have / performance / delighters / indifferent / reverse"
  default_prompt: "Use $kano-model when the task relates to the skill \"Kano Model\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
