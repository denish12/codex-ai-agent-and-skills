interface:
  display_name: "User Interview Script (B2B)"
  short_description: "Structured script for B2B interviews — separation of buyer and end-user, pain-discovery without leading questions"
  default_prompt: "Use $user-interview-script when the task relates to the skill \"User Interview Script (B2B)\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
