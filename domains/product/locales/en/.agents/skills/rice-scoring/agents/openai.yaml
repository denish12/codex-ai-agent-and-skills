interface:
  display_name: "RICE Scoring"
  short_description: "Prioritization by Reach × Impact × Confidence / Effort"
  default_prompt: "Use $rice-scoring when the task relates to the skill \"RICE Scoring\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
