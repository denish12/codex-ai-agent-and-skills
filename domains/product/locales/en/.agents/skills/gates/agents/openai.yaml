interface:
  display_name: "Gates — Quality Control of Product Pipeline Gates"
  short_description: "Quality control at gates — acceptance, rejection, severity (P0/P1/Note), escalation"
  default_prompt: "Use $gates when the task relates to the skill \"Gates — Quality Control of Product Pipeline Gates\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
