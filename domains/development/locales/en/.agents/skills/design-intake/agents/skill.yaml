version: 1
name: "design-intake"
display_name: "Design Intake"
description: "Find and analyze provided design materials (files/links), determine the \"source of truth\" for UI, and extract checkable rules (screens, components, tokens, states)."
default_prompt: "Use $design-intake when the task matches the \"Design Intake\" skill."
triggers:
  - "design-intake"
  - "design intake"
  - "Find and analyze provided design materials (files/links), determine the \"source of truth\" for UI, and extract checkable rules (screens, components, tokens, states)"
capabilities:
  - "design"
  - "intake"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
