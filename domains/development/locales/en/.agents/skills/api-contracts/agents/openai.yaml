interface:
  display_name: "API Contracts"
  short_description: "API contracts for UX flows — endpoints, schemas, errors, validation, authorization, perf/scalab…"
  default_prompt: "Use $api-contracts when the task matches the \"API Contracts\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
