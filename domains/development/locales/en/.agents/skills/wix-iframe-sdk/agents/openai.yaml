interface:
  display_name: "Wix iFrame SDK"
  short_description: "Practical skill on legacy Wix iFrame SDK (deprecated) — search for methods, events, parameters,…"
  default_prompt: "Use $wix-iframe-sdk when the task matches the \"Wix iFrame SDK\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
