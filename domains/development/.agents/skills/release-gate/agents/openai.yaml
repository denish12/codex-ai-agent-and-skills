interface:
  display_name: "Релизный гейт"
  short_description: "Финальный релиз-гейт — собрать отчёты Reviewer+Tester+CI, проверить DoD, классифицировать риски…"
  default_prompt: "Используй $release-gate, когда задача относится к навыку \"Релизный гейт\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
