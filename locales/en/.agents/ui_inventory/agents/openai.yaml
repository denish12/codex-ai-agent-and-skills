interface:
  display_name: "UI Components Inventory"
  short_description: "Compose an inventory of UI components and reuse rules — basic components, composite components,…"
  default_prompt: "Use $ui_inventory when the task matches the \"UI Components Inventory\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
