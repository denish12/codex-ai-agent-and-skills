version: 1
name: "a11y_baseline"
display_name: "Minimum baseline accessibility for web UI"
description: "Minimum baseline accessibility for web UI — keyboard navigation, focus management, form labels, ARIA for interactive components, error messages."
default_prompt: "Use $a11y_baseline when the task matches the \"Minimum baseline accessibility for web UI\" skill."
triggers:
  - "a11y_baseline"
  - "a11y baseline"
  - "Minimum baseline accessibility for web UI"
  - "Minimum baseline accessibility for web UI — keyboard navigation, focus management, form labels, ARIA for interactive components, error messages"
capabilities:
  - "a11y"
  - "baseline"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
