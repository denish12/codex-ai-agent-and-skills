{
  "name": "data_model",
  "display_name": "Data Model",
  "description": "Model data and strategy store — normalization/denormalization, indexes, transactions, cache, (optional) event sourcing/eventual consistency — with trade-offs and tied to UX flows.",
  "default_prompt": "Use $data_model when the task matches the \"Data Model\" skill.",
  "triggers": [
    "data_model",
    "data model",
    "Model data and strategy store — normalization/denormalization, indexes, transactions, cache, (optional) event sourcing/eventual consistency — with trade-offs and tied to UX flows"
  ],
  "capabilities": [
    "data",
    "model"
  ],
  "tools": [],
  "implicit_invocation": true
}
