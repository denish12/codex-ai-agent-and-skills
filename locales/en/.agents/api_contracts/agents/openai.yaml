interface:
  display_name: "API Contracts"
  short_description: "API contracts by UX flows — endpoints, schemas, errors, validation, authorization, perf/scalabi…"
  default_prompt: "Use $api_contracts when the task matches the \"API Contracts\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
