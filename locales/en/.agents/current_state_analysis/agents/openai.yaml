interface:
  display_name: "Current State Analysis: <project-name>"
  short_description: "Analysis of the current repository architecture ? conventions, patterns, tech debt, scaling bot…"
  default_prompt: "Use $current_state_analysis when the task matches the \"Current State Analysis: <project-name>\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
