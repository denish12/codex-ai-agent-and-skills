interface:
  display_name: "Deployment & CI Plan"
  short_description: "Deployment and CI plan — environments, configs, secrets, migrations, checks in CI (tests/lint/s…"
  default_prompt: "Use $deployment-ci-plan when the task matches the \"Deployment & CI Plan\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
