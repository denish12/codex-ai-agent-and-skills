interface:
  display_name: "QA API Contract Tests"
  short_description: "Check API on compliance contracts — schemas, codes, errors, validation, pagination; smoke via c…"
  default_prompt: "Use $qa_api_contract_tests when the task matches the \"QA API Contract Tests\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
