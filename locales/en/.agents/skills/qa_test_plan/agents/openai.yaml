interface:
  display_name: "QA Test Plan"
  short_description: "Create a QA test plan and coverage matrix from PRD and UX - roles, critical flows, UI states, n…"
  default_prompt: "Use $qa_test_plan when the task matches the \"QA Test Plan\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
