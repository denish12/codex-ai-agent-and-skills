interface:
  display_name: "QA Manual Run"
  short_description: "Manual execution of critical scenarios and regression — happy/edge/error paths based on UX Spec…"
  default_prompt: "Use $qa-manual-run when the task matches the \"QA Manual Run\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
