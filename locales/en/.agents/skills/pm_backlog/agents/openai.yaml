interface:
  display_name: "PM Backlog"
  short_description: "Decomposition of PRD into an initial backlog — epics, features, and tasks with priorities, depe…"
  default_prompt: "Use $pm_backlog when the task matches the \"PM Backlog\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
