interface:
  display_name: "PM Backlog"
  short_description: "Decompose the PRD into an initial backlog - epics, features, tasks, priorities, dependencies, a…"
  default_prompt: "Use $pm_backlog when the task matches the \"PM Backlog\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
