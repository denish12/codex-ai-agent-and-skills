interface:
  display_name: "Design Intake"
  short_description: "Find and analyze the provided design materials (files/links), determine the \"source of truth\" f…"
  default_prompt: "Use $design_intake when the task matches the \"Design Intake\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
