interface:
  display_name: "Observability Plan"
  short_description: "Create an observability plan - logs, metrics, traces, request correlation, log levels, alerts,…"
  default_prompt: "Use $observability_plan when the task matches the \"Observability Plan\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
