version: 1
name: "performance_review_baseline"
display_name: "N+1 indicators"
description: "Baseline perf-review — N+1, pagination, batches, extra requests, cache by architecture, heavy operations in the request path."
default_prompt: "Use $performance_review_baseline when the task matches the \"N+1 indicators\" skill."
triggers:
  - "performance_review_baseline"
  - "performance review baseline"
  - "N+1 indicators"
  - "Baseline perf-review — N+1, pagination, batches, extra requests, cache by architecture, heavy operations in the request path"
capabilities:
  - "performance"
  - "review"
  - "baseline"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
