interface:
  display_name: "N+1 indicators"
  short_description: "Baseline perf-review — N+1, pagination, batches, extra requests, cache by architecture, heavy o…"
  default_prompt: "Use $performance_review_baseline when the task matches the \"N+1 indicators\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
