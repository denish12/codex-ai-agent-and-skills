version: 1
name: "ui_inventory"
display_name: "UI Inventory"
description: "Compile an inventory of UI components and reuse rules — base components, composition components, patterns (forms, tables, dialogs), design tokens (minimum)."
default_prompt: "Use $ui_inventory when the task matches the \"UI Inventory\" skill."
triggers:
  - "ui_inventory"
  - "ui inventory"
  - "Compile an inventory of UI components and reuse rules — base components, composition components, patterns (forms, tables, dialogs), design tokens (minimum)"
capabilities:
  - "ui"
  - "inventory"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
