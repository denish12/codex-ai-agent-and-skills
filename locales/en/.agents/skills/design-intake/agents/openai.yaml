interface:
  display_name: "Design Intake"
  short_description: "Find and analyze provided design materials (files/links), determine the \"source of truth\" for U…"
  default_prompt: "Use $design-intake when the task matches the \"Design Intake\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
