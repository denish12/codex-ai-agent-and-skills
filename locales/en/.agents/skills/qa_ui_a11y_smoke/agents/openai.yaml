interface:
  display_name: "Missing alt on images"
  short_description: "UI/a11y smoke — keyboard/focus, labels/aria, basic contrast, states forms and lists, compliance…"
  default_prompt: "Use $qa_ui_a11y_smoke when the task matches the \"Missing alt on images\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
