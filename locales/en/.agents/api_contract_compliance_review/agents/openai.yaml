interface:
  display_name: "API Contract Compliance Review"
  short_description: "Check compliance implementation API contracts — schemas, codes errors, validation, authorizatio…"
  default_prompt: "Use $api_contract_compliance_review when the task matches the \"API Contract Compliance Review\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
