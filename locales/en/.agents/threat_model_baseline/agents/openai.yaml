interface:
  display_name: "Threat Model Baseline"
  short_description: "Threat baseline for MVP: assets, attack vectors, measures. Considers OWASP risks and roles/perm…"
  default_prompt: "Use $threat_model_baseline when the task matches the \"Threat Model Baseline\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
