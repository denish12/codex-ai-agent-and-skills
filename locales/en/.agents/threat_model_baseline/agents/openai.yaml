interface:
  display_name: "Threat Model Baseline"
  short_description: "Baseline threat model (threat baseline) for MVP — assets, attack vectors, mitigations. Includes…"
  default_prompt: "Use $threat_model_baseline when the task matches the \"Threat Model Baseline\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
