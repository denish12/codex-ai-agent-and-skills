interface:
  display_name: "QA API Contract Tests"
  short_description: "Check the API for compliance with contracts: schemes, codes, errors, validation, pagination; sm…"
  default_prompt: "Use $qa_api_contract_tests when the task matches the \"QA API Contract Tests\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
