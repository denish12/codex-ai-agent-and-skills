version: 1
name: "design_intake"
display_name: "Design Intake"
description: "Find and analyze the provided designs (files/links), determine \"source truth\" by UI and extract verifiable rules (screens, components, tokens, states)."
default_prompt: "Use $design_intake when the task matches the \"Design Intake\" skill."
triggers:
  - "design_intake"
  - "design intake"
  - "Find and analyze the provided designs (files/links), determine \"source truth\" by UI and extract verifiable rules (screens, components, tokens, states)"
capabilities:
  - "design"
  - "intake"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
