interface:
  display_name: "Testing Strategy (JS/TS)"
  short_description: "Testing strategy for JS/TS projects: unit (vitest/jest), integration (API/DB), e2e (playwright)…"
  default_prompt: "Use $testing_strategy_js when the task matches the \"Testing Strategy (JS/TS)\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
