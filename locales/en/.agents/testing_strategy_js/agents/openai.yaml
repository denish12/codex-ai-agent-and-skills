interface:
  display_name: "Testing Strategy (JS/TS)"
  short_description: "Strategy testing for JS/TS projects — unit (Vitest), integration (API/DB), e2e (Playwright). AA…"
  default_prompt: "Use $testing_strategy_js when the task matches the \"Testing Strategy (JS/TS)\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
