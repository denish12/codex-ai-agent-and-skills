interface:
  display_name: "UX Discovery"
  short_description: "Clarify UX introductory to PRD: roles, main flows, navigation, platforms (responsive), brand/re…"
  default_prompt: "Use $ux_discovery when the task matches the \"UX Discovery\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
