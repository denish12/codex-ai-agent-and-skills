interface:
  display_name: "UX Spec (Flows + Screens + States)"
  short_description: "Generate UX Spec: user flows, IA, list of screens, specification of each screen (actions/sectio…"
  default_prompt: "Use $ux_spec when the task matches the \"UX Spec (Flows + Screens + States)\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
