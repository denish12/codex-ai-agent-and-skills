version: 1
name: "ux_spec"
display_name: "UX Spec (Flows + Screens + States)"
description: "Form UX Spec — user flows, IA, list screens, specification each screen (actions/sections/validation/states), criteria UX quality for development and testing."
default_prompt: "Use $ux_spec when the task matches the \"UX Spec (Flows + Screens + States)\" skill."
triggers:
  - "ux_spec"
  - "ux spec"
  - "UX Spec (Flows + Screens + States)"
  - "Form UX Spec — user flows, IA, list screens, specification each screen (actions/sections/validation/states), criteria UX quality for development and testing"
capabilities:
  - "ux"
  - "spec"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "en"
  available_locales:
    - "ru"
    - "en"
