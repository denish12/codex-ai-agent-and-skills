interface:
  display_name: "QA Test Plan"
  short_description: "Compose test plan and coverage matrix by PRD/UX — roles, critical user flows, states UI, negati…"
  default_prompt: "Use $qa_test_plan when the task matches the \"QA Test Plan\" skill."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
