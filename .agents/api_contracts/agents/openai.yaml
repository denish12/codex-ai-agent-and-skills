interface:
  display_name: "API-контракты"
  short_description: "API контракты по UX flows — эндпоинты, схемы, ошибки, валидация, авторизация, perf/scalability…"
  default_prompt: "Используй $api_contracts, когда задача относится к навыку \"API-контракты\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
