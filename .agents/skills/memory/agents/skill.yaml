version: 1
name: "memory"
display_name: "Память и векторная БД"
description: "Управление контекстом проекта — что сохранять, как извлекать, как избегать конфликтов решений и потери контекста между сессиями."
default_prompt: "Используй $memory, когда задача относится к навыку \"Память и векторная БД\"."
triggers:
  - "memory"
  - "Память и векторная БД"
  - "Управление контекстом проекта — что сохранять, как извлекать, как избегать конфликтов решений и потери контекста между сессиями"
capabilities:
  - "memory"
tools: []
invocation:
  explicit: true
  implicit: true
localization:
  default_locale: "ru"
  available_locales:
    - "ru"
    - "en"
