interface:
  display_name: "Память и векторная БД"
  short_description: "Управление контекстом проекта через векторную БД/MCP: что сохранять, как извлекать, как избегат…"
  default_prompt: "Используй $memory, когда задача относится к навыку \"Память и векторная БД\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
