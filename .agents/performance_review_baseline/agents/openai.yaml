interface:
  display_name: "Базовая проверка производительности"
  short_description: "Базовый perf-ревью: N+1, пагинация, батчи, лишние запросы, кеш по архитектуре, тяжелые операции…"
  default_prompt: "Используй $performance_review_baseline, когда задача относится к навыку \"Базовая проверка производительности\"."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
